---
- hosts: REPLICATION
  remote_user: "{{ lookup('env', 'REMOTE_WEB_USER') }}"
  tasks:
  - name: 'Copying files'
    copy:
      src: "{{ item }}"
      dest: /var/www/siteshir-fleetviewer/userfiles/Mod1
    with_fileglob:
      - ./*.colset

