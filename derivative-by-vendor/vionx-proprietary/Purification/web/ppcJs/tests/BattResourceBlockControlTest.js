dojo.provide('ppcJs.tests.BattResourceBlockControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.BattResourceBlockControlTest', dojo.moduleUrl('ppcJs', 'tests/BattResourceBlockControlTest.html'), 20000);
}