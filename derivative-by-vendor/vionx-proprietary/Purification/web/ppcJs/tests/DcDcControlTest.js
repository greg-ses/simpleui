dojo.provide('ppcJs.tests.DcDcControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.DcDcControlTest', dojo.moduleUrl('ppcJs', 'tests/DcDcControlTest.html'), 20000);
}