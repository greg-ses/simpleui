dojo.provide('ppcJs.tests.AjaxStatusControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.AjaxStatusControlTest', dojo.moduleUrl('ppcJs', 'tests/AjaxStatusControlTest.html'), 20000);
}