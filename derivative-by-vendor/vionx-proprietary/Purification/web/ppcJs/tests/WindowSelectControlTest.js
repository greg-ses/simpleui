dojo.provide('ppcJs.tests.WindowSelectControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.WindowSelectControlTest', dojo.moduleUrl('ppcJs', 'tests/WindowSelectControlTest.html'), 20000);
}