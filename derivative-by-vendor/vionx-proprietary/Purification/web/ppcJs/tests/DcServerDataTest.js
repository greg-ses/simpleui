dojo.provide('ppcJs.tests.DcServerDataTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.DcServerDataTest', dojo.moduleUrl('ppcJs', 'tests/DcServerDataTest.html'), 20000);
}