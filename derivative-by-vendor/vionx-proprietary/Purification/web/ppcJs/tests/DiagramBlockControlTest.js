dojo.provide('ppcJs.tests.DiagramBlockControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.DiagramBlockControlTest', dojo.moduleUrl('ppcJs', 'tests/DiagramBlockControlTest.html'), 20000);
}