TO UPDATE USING ANSIBLE 2.4.1.0:

    Make sure you can log into Crystal device or VM to be updated without password using ssh.
    To do this do:
    ssh-copy-id service@SOME_VEC_HOSTNAME

    Verify you can log in without password using:
    ssh service@SOME_VEC_HOSTNAME

    add a group containing the SOME_VEC_HOSTNAME to /etc/ansible/hosts

    ansible <host-pattern> --module-name=arm-web-setup.yml

    Example:

    ansible-playbook --limit=SOME_VEC_HOSTNAME --user=service --ask-pass --become-method=su --ask-become-pass --syntax-check arm-web-setup.yml
    ansible-playbook --limit=SOME_VEC_HOSTNAME --user=service --ask-pass --become-method=su --ask-become-pass                arm-web-setup.yml



(ALTERNATE INSTRUCTIONS) - TO UPDATE USING SH FROM DEVICE (VEC) SERVER:

    If you prefer to update without using ansible, follow these steps:

        To install on the DEVICE server:

        (Log into DEVICE server)
        cd /home/service
        scp DEVELOPER_USERNAME@DEVELOPER_HOSTNAME:/home/DEVELOPER_USERNAME/GIT_FOLDER/vionx_linux/target/BMSServer/setup/vec-web-setup/arm-web-setup-bundle.tgz ./
        tar xzvf arm-web-setup-bundle.tgz
	su -
	cd ~/arm-staging-area
        sh ./arm-web-setup.sh ~/arm-staging-area

