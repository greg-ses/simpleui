dojo.provide('ppcJs.tests.LogControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.LogControlTest', dojo.moduleUrl('ppcJs', 'tests/LogControlTest.html'), 20000);
}