dojo.provide('ppcJs.tests.iPhoneButtonTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.iPhoneButtonTest', dojo.moduleUrl('ppcJs', 'tests/iPhoneButtonTest.html'), 20000);
}