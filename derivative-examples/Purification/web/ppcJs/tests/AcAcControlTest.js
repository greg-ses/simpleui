dojo.provide('ppcJs.tests.AcAcControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.AcAcControlTest', dojo.moduleUrl('ppcJs', 'tests/AcAcControlTest.html'), 20000);
}