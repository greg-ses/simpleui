dojo.provide('ppcJs.tests.BinaryRadioButtonTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.BinaryRadioButtonTest', dojo.moduleUrl('ppcJs', 'tests/BinaryRadioButtonTest.html'), 20000);
}