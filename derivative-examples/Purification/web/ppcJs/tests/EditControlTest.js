dojo.provide('ppcJs.tests.EditControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.EditControlTest', dojo.moduleUrl('ppcJs', 'tests/EditControlTest.html'), 20000);
}