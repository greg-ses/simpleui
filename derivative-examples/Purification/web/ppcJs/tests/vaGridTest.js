dojo.provide('ppcJs.tests.vaGridTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.vaGridTest', dojo.moduleUrl('ppcJs', 'tests/vaGridTest.html'), 20000);
}