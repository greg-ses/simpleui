dojo.provide('ppcJs.tests.LockedCheckBoxTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.LockedCheckBoxTest', dojo.moduleUrl('ppcJs', 'tests/LockedCheckBoxTest.html'), 20000);
}