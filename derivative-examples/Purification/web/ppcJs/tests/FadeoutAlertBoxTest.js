dojo.provide('ppcJs.tests.FadeoutAlertBoxTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.FadeoutAlertBoxTest', dojo.moduleUrl('ppcJs', 'tests/FadeoutAlertBoxTest.html'), 20000);
}