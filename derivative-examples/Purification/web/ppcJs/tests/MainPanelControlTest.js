dojo.provide('ppcJs.tests.MainPanelControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.MainPanelControlTest', dojo.moduleUrl('ppcJs', 'tests/MainPanelControlTest.html'), 20000);
}