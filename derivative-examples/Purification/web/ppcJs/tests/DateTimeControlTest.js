dojo.provide('ppcJs.tests.DateTimeControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.DateTimeControlTest', dojo.moduleUrl('ppcJs', 'tests/DateTimeControlTest.html'), 20000);
}