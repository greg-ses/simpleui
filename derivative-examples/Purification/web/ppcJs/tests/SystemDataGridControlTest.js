dojo.provide('ppcJs.tests.SystemDataGridControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.SystemDataGridControlTest', dojo.moduleUrl('ppcJs', 'tests/SystemDataGridControlTest.html'), 20000);
}