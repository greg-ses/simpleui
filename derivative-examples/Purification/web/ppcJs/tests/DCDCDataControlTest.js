dojo.provide('ppcJs.tests.DCDCDataControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.DCDCDataControlTest', dojo.moduleUrl('ppcJs', 'tests/DCDCDataControlTest.html'), 20000);
}