dojo.provide('ppcJs.tests.InsertCommandMenuControlTest');

if (dojo.isBrowser) {
    //Define the HTML file/module URL to import as a 'remote' test.
    doh.registerUrl('ppcJs.tests.InsertCommandMenuControlTest', dojo.moduleUrl('ppcJs', 'tests/InsertCommandMenuControlTest.html'), 20000);
}